import { SCREEN_WIDTH, SCREEN_HEIGHT, TICK_INTERVAL } from "./constants";

// Виводимо в консоль щоб переконатись що все правильно
console.log('Screen:', SCREEN_WIDTH, 'x', SCREEN_HEIGHT);
console.log('Tick interval:', TICK_INTERVAL, 'ms');